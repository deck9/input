# No Interaction

The No Interaction input type is used to display a message to the user without any input.

This interaction type will not appear in the submission data, since no interaction is happening.
